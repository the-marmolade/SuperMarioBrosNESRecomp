{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:update": "cross-env UPDATE_BASELINE=1 vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.1"
  }
}
